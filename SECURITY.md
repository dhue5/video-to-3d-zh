# 安全政策

## API 密钥

请不要在 Issue、Pull Request、截图或日志中公开 API 密钥。发现密钥泄露时，应立即在对应服务商后台撤销并重新生成。

插件的 API 密钥字段使用密码显示；只有用户主动勾选“保存接口密钥”时才会写入本机 Blender 用户偏好，不写入 `.blend` 文件或仓库源码。

## 报告问题

请不要在公开 Issue 中粘贴完整密钥或未经脱敏的接口响应。对于可能导致凭据泄露的问题，请先删除敏感信息，再提交 Issue。

